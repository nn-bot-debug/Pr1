# 🐾 Animal Management System

Це навчальний проєкт на Java для демонстрації основ ООП та особливостей роботи з пам'яттю.

## 📖 Що робить програма
* Моделює ієрархію тварин (`Animal`, `Dog`, `Cat`) з використанням наслідування.
* Демонструє **поліморфізм**: тварини мають спільну дію `eat()`, але виконують її по-різному.
* Реалізує безпечне приведення типів (**Casting**) через перевірку `instanceof`.
* Тестує різницю між порівнянням об'єктів через `==` та `.equals()`.
* Показує роботу **Integer Cache** (кешування чисел від -128 до 127).

## 🛠 Технології
* **Java 15+** (використано метод `.formatted()` для рядків).
* **IntelliJ IDEA** — середовище розробки.

## 💻 Як запустити
1. Скомпілюйте файли в папці проєкту.
2. Запустіть клас `Main.java`.
